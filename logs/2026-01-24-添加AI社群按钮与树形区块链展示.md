## 用户需求

- 在首页新增按钮【AI社群】，点击后进入一个可视化的树形“区块链”结构展示（类似图1）。
- 点击每个区块弹出相应信息（联系人等），信息源在飞书文档：`https://my.feishu.cn/wiki/TRIGw4cYVipSx5k58kHccB3xnsb`

## 我做了什么（主要行动）

- 在 `index.html` 顶部按钮区新增入口：`AI社群`（支持中英文切换文案）。
- 新增 `AI社群`全屏弹窗（沿用现有 `modal-overlay` 体系），内部渲染树形结构 + SVG 自动连线（带向上箭头，贴近图1“根在下”的结构）。
- 新增“节点信息”弹窗：点击任意区块打开，展示 **说明/联系人/链接**；默认会给出“飞书信息源”按钮，方便后续补齐信息。
- 兼容性加固：为 `CSS.escape` 增加 fallback，避免部分浏览器缺失导致连线定位失败。

## 代码位置

- 入口按钮：`index.html` header 按钮组（`#aiCommunityButton`）
- 社群弹窗：`#aiCommunityModal`
- 节点信息弹窗：`#communityNodeModal`
- 主要逻辑模块：`// AI Community (Tree/Blockchain) Module`（包含示例数据 `COMMUNITY_TREE`）
- 文案：`translations.zh/en` 增加 `aiCommunity / aiCommunityTitle / aiCommunityHint`

## 说明与后续补齐

- 飞书链接为外部受控内容，网页端无法直接“自动读取”飞书 wiki（通常需要登录/鉴权）。当前实现先把信息源链接挂在每个节点的弹窗里，并预留好 `contacts/desc/links` 字段，后续可手动补齐或再接入后端接口同步飞书内容。

